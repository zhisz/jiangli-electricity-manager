# 安全说明

## 私密配置

请勿向仓库提交以下真实值：

- 校付宝 `shiroJID`；
- 正式更新清单地址和匿名统计服务地址；
- SSH 私钥、服务器 IP、生产域名和真实部署目录；
- 后台密码摘要、会话密钥、遥测 HMAC 密钥；
- 正式 APK、生产更新清单和域名所有权校验文件。

Android 私密构建参数应放在已被 Git 忽略的 `local.properties`。服务端密钥应只通过
部署环境变量提供。公开示例中的域名、目录和密钥均为占位符。

## 报告问题

如发现可能造成凭据泄露、支付流程绕过或用户数据暴露的问题，请不要先公开利用细节，
可发送邮件至 `3357627169@qq.com` 联系开发者 zhiSZ。
